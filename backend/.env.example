# Render: definir en Dashboard > Environment

# Puerto (Render lo asigna automáticamente)
PORT=3000

# Supabase (crear proyecto en supabase.com y usar Service Role key para el resolver)
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Panel de administración (interfaz para gestionar clientes). Si no se define, /admin devuelve 503.
ADMIN_PASSWORD=tu-contraseña-segura

# Dominios permitidos para redirect (evitar open redirect). Separados por coma.
ALLOWED_REDIRECT_HOSTS=mic-saas-fontend.onrender.com,tu-app.onrender.com,localhost
